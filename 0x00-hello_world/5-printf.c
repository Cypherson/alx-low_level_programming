#include <stdio.h>

/**
 * main - entry point
 *
 * Description: prints a string to stdout using printf function
 *
 * Return: always 0 on success
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
