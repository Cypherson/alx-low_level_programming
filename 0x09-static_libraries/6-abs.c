#include "main.h"
/**
 * Description: _abs - computes the absolute value of an integer.
 *
 * @abs: input data
 *
 * Return: success
 *
 */
int _abs(int ab)
{
return 
	(ab * ((ab > 0) - (ab < 0)));
}
