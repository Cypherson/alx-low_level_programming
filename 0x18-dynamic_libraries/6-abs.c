#include "main.h"
/**
 * Description: _abs - computes the absolute value of an integer.
 * @ab: variable that holds integer
 * Return: success
 */
int _abs(int ab)
{
return
	(ab * ((ab > 0) - (ab < 0)));
}
